export class Calcify{
    public x: number;
    public y: number;

    constructor(x: number, y: number){
        this.x = x;
        this.y = y;
    }
    public add(){
        return this.x+this.y;
    }
}